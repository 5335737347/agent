[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mx", email = "mx18782708114@163.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.3.1",
    "python-dotenv>=1.2.2",
]

[project.scripts]
agent = "agent.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
